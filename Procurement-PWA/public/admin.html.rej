diff a/Procurement-PWA/public/admin.html b/Procurement-PWA/public/admin.html	(rejected hunks)
@@ -169,6 +170,7 @@
           <select id="role-${req.id}">
             <option value="Staff">Staff</option>
             <option value="Supervisor">Supervisor</option>
+            <option value="Manager">Manager (placeholder — Work Sites feature)</option>
             <option value="Executive">Executive</option>
           </select>
           <select id="dept-${req.id}">
@@ -197,6 +199,7 @@
           <select id="role-user-${user.id}">
             <option value="Staff" ${user.role === 'Staff' ? 'selected' : ''}>Staff</option>
             <option value="Supervisor" ${user.role === 'Supervisor' ? 'selected' : ''}>Supervisor</option>
+            <option value="Manager" ${user.role === 'Manager' ? 'selected' : ''}>Manager (placeholder — Work Sites feature)</option>
             <option value="Executive" ${user.role === 'Executive' ? 'selected' : ''}>Executive</option>
           </select>
           <select id="dept-user-${user.id}">
